export default function exercicio6 ({quantiadade, preco})
{
    let subtotal= (quantiadade, preço)
    let desconto= (subtotal*0.1)
    let valorf= (subtotal-desconto)

    return (

    <div>
       Total: {subtotal} </br>
       Desconto: R${desconto} </br>
       Valor Final: R${valorf} </br>
    </div>  

    );
    }  